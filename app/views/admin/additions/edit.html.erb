<div class="page-bar">
  <%= breadcrumb(link_to('商品加購', admin_additions_path), current_object.purchase_product.try(:title)) %>
</div>
<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h2 class="page-header">
        修改加購商品
      </h2>
      <div class="portlet box blue">
        <div class="portlet-title">
          <div class="caption">
            <%= current_object.purchase_product.try(:title) %>
          </div>
        </div>
        <div class="portlet-body form">
          <%= render 'form' %>
        </div>
      </div>
    </div>
  </div>
</div>
